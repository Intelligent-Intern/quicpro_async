/*
 * =========================================================================
 * FILENAME:   include/config/semantic_geometry/default.h
 * PROJECT:    quicpro_async
 * AUTHOR:     Jochen Schultz <jschultz@php.net>
 *
 * WELCOME:    They see only their own shadows, or the shadows of one another,
 * which the fire throws on the opposite wall of the cave.
 *
 * PURPOSE:
 * This header declares the function for loading the module's hardcoded
 * default values for the Semantic Geometry engine.
 * =========================================================================
 */

#ifndef QUICPRO_CONFIG_SEMANTIC_GEOMETRY_DEFAULT_H
#define QUICPRO_CONFIG_SEMANTIC_GEOMETRY_DEFAULT_H

/**
 * @brief Loads the hardcoded, default values into the module's config struct.
 */
void qp_config_semantic_geometry_defaults_load(void);

#endif /* QUICPRO_CONFIG_SEMANTIC_GEOMETRY_DEFAULT_H */
