/*
 * =========================================================================
 * FILENAME:   include/config/app_http3_websockets_webtransport/default.h
 * PROJECT:    quicpro_async
 * AUTHOR:     Jochen Schultz <jschultz@php.net>
 *
 * WELCOME:    There are countless ingredients that make up the human body
 * and mind, like all the components that make up me as an
 * individual with my own personality.
 *
 * PURPOSE:
 * This header declares the function for loading the module's hardcoded
 * default values for application protocols.
 * =========================================================================
 */

#ifndef QUICPRO_CONFIG_APP_PROTOCOLS_DEFAULT_H
#define QUICPRO_CONFIG_APP_PROTOCOLS_DEFAULT_H

/**
 * @brief Loads the hardcoded, default values into the module's config struct.
 */
void qp_config_app_http3_websockets_webtransport_defaults_load(void);

#endif /* QUICPRO_CONFIG_APP_PROTOCOLS_DEFAULT_H */
